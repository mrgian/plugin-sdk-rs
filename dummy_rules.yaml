- rule: Sample dummy-rs plugin rule
  desc: All events
  condition: (evt.num>=0)
  output: -> rust plugin event (num=%evt.num pluginname=%evt.pluginname)
  priority: CRITICAL
  source: dummy-rs-source
